# spin-up
ansible-playbook ./site.yml -i ./inventory/my-cluster/hosts.ini

# copy config
scp daanscheper@192.168.2.222:~/.kube/config ~/.kube/config

# reset
ansible-playbook ./reset.yml -i ./inventory/my-cluster/hosts.ini
